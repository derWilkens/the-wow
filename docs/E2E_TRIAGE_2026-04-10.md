# E2E Triage 2026-04-10

## Gesamtstand
- Backend unit: gruen
- Frontend unit/component: gruen
- Playwright E2E: 41 passed, 22 failed, 1 skipped

## Cluster A: Reload- und Landing-Annahmen in Tests veraltet
Betroffene Specs:
- canvas-view.spec.ts
- export.spec.ts
- workflow-hierarchy.spec.ts (Reload-Szenario)
- it-tools.spec.ts (Reload-Szenario)
- edge-attributes.spec.ts (Reload-Szenario)
- data-object-edge.spec.ts (Reload-Szenario)

Befund:
- Nach `page.reload()` landet die App nicht mehr direkt auf der Workflow-Auswahl, sondern oft auf der Organisationsauswahl.
- Der Fehler ist in den Failure-Artefakten eindeutig sichtbar, z. B. `canvas-view.spec.ts`.
- Das ist primaer ein Test-Harness-Problem, nicht zwingend ein Produktfehler.

Technische Ursache:
- Mehrfirmen-/Mandanten-Landing ist heute ein vorgelagerter Zustand.
- Mehrere E2E-Tests gehen noch von einem aelteren Post-Reload-Pfad aus.

Empfohlene Reparatur:
- Reload-Helfer zentralisieren.
- Nach Reload zuerst Organisation waehlen, dann Workflow oeffnen.
- Vorzugsweise in `frontend/e2e/helpers.ts` statt einzeln pro Spec.

Prioritaet:
- Hoch, weil dieser Cluster viele Failures erzeugt.

## Cluster B: Feature-Gates standardmaessig aus, Tests erwarten Sichtbarkeit
Betroffene Specs:
- sipoc-view.spec.ts
- swimlane-visible-roles.spec.ts
- screenshots.spec.ts (Role-Lanes-Screenshot)

Befund:
- `sipoc-view.spec.ts` wartet auf `toolbar-view-sipoc`, das im Snapshot gar nicht vorhanden ist.
- Das passt zum aktuellen Produktverhalten: Tabellensicht und Swimlane View sind per UI-Preference standardmaessig deaktiviert.

Technische Ursache:
- Tests aktivieren die noetigen UI-Praeferenzen vor der Interaktion nicht.

Empfohlene Reparatur:
- Gemeinsamen E2E-Helfer einfuehren, der `enable_table_view` und/oder `enable_swimlane_view` vor dem eigentlichen Testfluss aktiviert.
- Screenshot-Spec ebenfalls darueber routen.

Prioritaet:
- Hoch, weil dieser Cluster funktional klar ist und viele Folgefehler beseitigt.

## Cluster C: Edge-/Datenobjekt-Flows wurden produktseitig geaendert, Tests sind veraltet
Betroffene Specs:
- data-object-edge.spec.ts (Hint, Single-Object-Click, Aggregate-Hover, Reload)
- screenshots.spec.ts (Edge/Data-Object/Settings)
- bim-cyclic-coordination.spec.ts (erster Datenobjekt-Fall)

Befund:
- Beim Klick auf ein einzelnes Kanten-Datenobjekt oeffnet jetzt direkt der Datenobjekt-Dialog.
- Der alte Test erwartet noch ein `edge-data-object-popover-*`.
- In der Screenshot-Spec wird `edge-transport-mode-trigger` erwartet, waehrend der Snapshot bereits den Datenobjekt-Dialog zeigt.

Technische Ursache:
- Produktverhalten wurde bewusst auf Direktoeffnen des Datenobjekt-Dialogs umgestellt.
- E2E-Tests und Capture-Spec bilden noch das alte Zwischen-Popover ab.

Empfohlene Reparatur:
- `data-object-edge.spec.ts` auf neues Zielbild umstellen.
- `screenshots.spec.ts` in diesem Block neu ordnen: zuerst Edge-Detail gezielt oeffnen, dann separaten Klick aufs Datenobjekt fuer den Datenobjekt-Dialog.
- `bim-cyclic-coordination.spec.ts` auf robustere Assertions anpassen; kein Vertrauen auf alte Inline-/Title-Darstellung allein.

Prioritaet:
- Hoch, weil diese Aenderung direkt die Screenshot-Regeneration blockiert.

## Cluster D: Rollen-Picker-Flow wurde komprimiert, Tests erwarten alte Create-Form
Betroffene Specs:
- activity-role-badge.spec.ts
- Teile von settings-master-data.spec.ts

Befund:
- `activity-role-badge.spec.ts` versucht direkt `activity-role-create-name-*` zu fuellen.
- Im Snapshot ist aber nur der kompakte Rollen-Dropdown sichtbar mit Button `Neue Rolle anlegen`.
- Die Create-Form oeffnet sich erst nach explizitem Klick.

Technische Ursache:
- Produktverhalten wurde bewusst kompakter gemacht.
- Test wartet auf ein Element, das ohne den Zwischenklick nie erscheint.

Empfohlene Reparatur:
- Test auf den neuen Zweischritt umstellen:
  1. Rollen-Popover oeffnen
  2. `Neue Rolle anlegen` klicken
  3. Danach Create-Form bedienen
- Settings-Spec auf Akronym-Badge/kompakten Rollenfluss pruefen.

Prioritaet:
- Mittel bis hoch.

## Cluster E: Neu eingefuegte Inline-Edit-/Dialog-Zustaende machen bestehende Testannahmen fragil
Betroffene Specs:
- connection-target-preview.spec.ts
- activity-detail-check-sources.spec.ts
- activity-detail-from-edge-detail.spec.ts
- gateways.spec.ts
- Teile von screenshots.spec.ts

Befund:
- In mehreren Snapshots ist `Neue Aktivitaet` noch als aktives Textfeld offen.
- Das deutet auf einen hängenden Inline-Rename-/Detailzustand hin, waehrend der Test bereits Folgeaktionen erwartet.
- `connection-target-preview.spec.ts` scheitert schon an der Vorbedingung, weil ein Zielhandle sichtbar bleibt.
- `activity-detail-check-sources.spec.ts` endet ohne sichtbaren Detaildialog, obwohl der Flow ihn spaetestens dort erwartet.

Technische Ursache:
- Mehrere neuere UI-Aenderungen greifen ineinander: Inline-Rename, Autosave, Detailwechsel, kompakte Node-Aktionen.
- Einige E2E-Helfer verlassen sich noch auf aeltere stabile Zwischenzustände.

Empfohlene Reparatur:
- E2E-Helfer haerten:
  - nach Node-Einfügen explizit warten, bis Inline-Edit beendet ist
  - Detaildialog oeffnen ueber robusten Helfer statt implizite Node-Doppelklick-Annahmen
  - Handle-Sichtbarkeit nur nach explizitem Deselektionsschritt messen
- Bei `connection-target-preview.spec.ts` zusaetzlich pruefen, ob der dritte Node initial zu nah am ersten/zweiten liegt und deshalb legitim im Preview-Radius ist.

Prioritaet:
- Mittel. Hier steckt eher Testfragilitaet als ein einziger klarer Produktbug.

## Empfohlene Reihenfolge
1. Cluster A reparieren: Reload-/Landing-Helfer zentral in `helpers.ts`
2. Cluster B reparieren: View-Preferences in betroffenen Specs zentral aktivieren
3. Cluster C reparieren: Edge-/Datenobjekt-Specs und Screenshot-Spec an aktuelles Produktverhalten anpassen
4. Cluster D reparieren: Rollen-Picker-E2Es auf den neuen kompakten Flow umstellen
5. Cluster E isolieren: verbleibende fragilen Specs einzeln nachziehen

## Erwarteter Effekt
- Mit A + B + C sollte der groesste Teil der 22 Failures verschwinden.
- D ist ein kleiner, klarer Follow-up.
- E enthaelt die wahrscheinlich verbleibenden echten Regressions- oder Synchronisationsprobleme.
